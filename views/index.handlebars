<h1>Burgers</h1>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
    <h1>{{this.burger_name}}<button class= "devour" data-burger={{this.id}}>DEVOUR!</button></h1>
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers I have devoured</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
    <h1>{{this.burger_name}}<button class= "" data-burger={{this.id}}>COOK AGAIN!</button></h1>
    {{/if}}
  {{/each}}
</ul>

<h2>New Recipe!</h2>
<form class="create-form">
  <div class="form-group">
    <label for="burger_input">Burger Name:</label>
    <input type="text" id= "burger_name">
  </div>

  <button type="submit">Add Burger</button>
</form>